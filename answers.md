Jaret Solomon

Place all written answers from recitation-08.md here for easier grading.

1b) work = O((V + E) log V); span = O(log V * V)

2b)
If  parent dictionary = {'a': 's', 'b': 's', 'c': 'b', 'd': 'c'} and destination = 'd', the function returns 'sbc', which represents the path from the source to the destination
time complexity is proportional to the # of total nodes in the path, or O(L) when L = path length.